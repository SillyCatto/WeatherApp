/** @type {import('tailwindcss').Config} */
export const content = [
  "./App/index.html",        // Include the main HTML file
  "./App/src/**/*.{js,jsx}", // Include all JavaScript and JSX files in the src/ folder and subfolders
];
export const theme = {
  extend: {}, // Use this to customize the theme if needed
};
export const plugins = [];
